cat > render.yaml <<EOF
services:
  - type: web
    name: mnjoro-app
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    autoDeploy: true
EOF

git add render.yaml
git commit -m "Force install devDependencies"
git push

